pool:
  vmImage: ubuntu-latest

steps:
- bash: |
        echo "Starting pipeline"
        echo "This is a test run"
        ls
        pwd
        /bin/bash vhdbuilder/scripts/automate.sh
  displayName: 'test pipeline setup'