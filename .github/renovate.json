{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "enabledManagers": ["custom.regex"],
  "prConcurrentLimit": 50,
  "prHourlyLimit": 20,
  "branchConcurrentLimit": 50,
  "logLevelRemap": [
    {
      "matchMessage": "/^Custom manager fetcher/",
      "newLogLevel": "info"
    }
  ],
  "packageRules": [
    {
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "automerge": false
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "assignees": ["devinwong"]
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "parts/linux/cloud-init/artifacts/test_components.json"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        ".*?\"downloadURL\":\\s*\"(?<registryUrl>[^/]+)/(?<packageName>[^:]+)[\\s\\S]*?\"multiArchVersions\": \\[\\s*\"(?<currentValue>[^\\\"]+)\".*"
      ],
      "datasourceTemplate": "docker",
      "registryUrlTemplate": "https://mcr.microsoft.com"
    },
    {
      "customType": "regex",
      "fileMatch": ["parts/linux/cloud-init/artifacts/test_components.json"],
      "matchStrings": [
        "\"name\":\\s*\"(?<packageName>containerd)\"[^}]*\"current\"[^}]*\"versions\":\\s*\\[\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "versioningTemplate": "semver-coerced",
      "datasourceTemplate": "custom.containerd"
    },
    {
      "customType": "regex",
      "fileMatch": [
        "parts/linux/cloud-init/artifacts/test_components.json"
      ],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "\"name=(?<packageName>[^ ]+) os=ubuntu release=(?<testname>\\d{2}\\.\\d{2})\",\\s*\"versions\":\\s*\\[\\s*\"(?<currentValue>[^\"]+)\""

      ],
      "datasourceTemplate": "custom.moby-runc",
      "versioningTemplate": "semver-coerced"
    }
  ],
  "customDatasources": {
    "moby-runc": {
      "defaultRegistryUrlTemplate": "https://packages.microsoft.com/ubuntu/{{testname}}/prod/dists/jammy/main/binary-amd64/Packages",
      "format": "plain",
      "transformTemplates": [
        "{\"releases\": $map(($index := releases#$i[version=\"Package: moby-runc\"].$i; $map($index, function($i) { $replace(releases[$i + 1].version, /^Version:\\s*/, \"v\") })), function($v) { {\"version\": $v} })}"
      ]
    },
    "containerd": {
      "defaultRegistryUrlTemplate": "https://packages.microsoft.com/ubuntu/22.04/prod/dists/testing/main/binary-amd64/Packages",
      "format": "plain",
      "transformTemplates": [
        "{\"releases\": $map(($index := releases#$i[version=\"Package: moby-containerd\"].$i; $map($index, function($i) { $replace(releases[$i + 1].version, /^Version:\\s*/, \"v\") })), function($v) { {\"version\": $v} })}"
      ]
    }
  }
}
