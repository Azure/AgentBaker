#!/bin/bash
# Mock kubectl for LocalDNS enabled scenario
case "$*" in
    *"get node"*"jsonpath"*)
        echo "enabled"
        ;;
    *)
        echo "Mock kubectl: unknown command: $*" >&2
        exit 1
        ;;
esac
