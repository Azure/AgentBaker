#!/bin/bash
# Mock systemctl for failed service
case "$*" in
    *"is-active localdns.service"*)
        echo "failed"
        exit 3
        ;;
    *)
        echo "Mock systemctl: unknown command: $*" >&2
        exit 1
        ;;
esac
