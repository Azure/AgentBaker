#!/bin/bash
if [[ "$*" == *"cat kubelet"* ]]; then
    cat <<'SYSTEMCTL_EOF'
[Service]
ExecStart=/usr/local/bin/kubelet \
  --container-runtime-endpoint="unix:///run/containerd/containerd.sock" \
  --v=2
SYSTEMCTL_EOF
else
    exit 0
fi
